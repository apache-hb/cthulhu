subdir('target')

if target_cfamily.allowed()
    subdir('cfamily')
endif

if target_debug.allowed()
    subdir('debug')
endif

if target_hlsl.allowed()
    subdir('hlsl')
endif

if target_llvm.allowed()
    subdir('llvm')
endif
