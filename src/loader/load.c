#include "common.h"

void begin_load(data_t *in, reports_t *reports, const format_t *format, const char *path) {
    begin_data(in, reports, format, path);
}

void end_load(data_t *in) {

}

bool read_entry(data_t *in, index_t index, record_t *record) {

}
