#include "enum_modules.h"

@EXTERNS@

static_modules_t get_static_modules(void)
{
    static_modules_t langs = {
        .langs = { @LANGS@ },
        .plugins = { @PLUGINS@ },
        .targets = { @TARGETS@ },
    };

    return langs;
}
