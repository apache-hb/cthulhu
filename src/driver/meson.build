# little driver support library
subdir('driver')

subdir('c_family')

if lang_ctu.allowed()
    subdir('ctu')
endif

if lang_pl0.allowed()
    subdir('pl0')
endif

if lang_oberon.allowed()
    subdir('oberon')
endif

if lang_jvm.allowed()
    subdir('jvm')
endif

if lang_example.allowed()
    subdir('example')
endif