#include "support/langs.h"

#include "memory/memory.h"

@EXTERNS@

static const language_t *const kLangArray[] = { @MODS@ };

langs_t get_langs(void)
{
    langs_t ret = {
        .langs = kLangArray,
        .size = (sizeof(kLangArray) / sizeof(language_t*))
    };
    return ret;
}
