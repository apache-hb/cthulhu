if lang_ctu.allowed()
    subdir('ctu')
endif

if lang_pl0.allowed()
    subdir('pl0')
endif

if lang_oberon.allowed()
    subdir('oberon')
endif

if lang_example.allowed()
    subdir('example')
endif

if lang_cc.allowed()
    subdir('cfamily/cc')
endif
