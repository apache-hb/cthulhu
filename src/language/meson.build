if lang_cpp.allowed()
    subdir('c_family/pre')
endif

if lang_c.allowed()
    subdir('c_family/lang')
endif

if lang_ctu.allowed()
    subdir('ctu')
endif

if lang_pl0.allowed()
    subdir('pl0')
endif

if lang_oberon.allowed()
    subdir('oberon')
endif

if lang_jvm.allowed()
    subdir('jvm')
endif

if lang_example.allowed()
    subdir('example')
endif