#include "argparse/argparse.h"

#include "base/macros.h"

arg_parse_result_t arg_parse(const arg_parse_config_t *config)
{
    arg_parse_result_t result = { 0 };
    UNUSED(config);

    return result;
}
