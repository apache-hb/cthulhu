@interop("windows.h", "user32")
def MessageBoxA(hWnd: *void, lpText: str, lpCaption: str, uType: uint): int;

export def main(argc: int, argv: *str): int {
    MessageBoxA(0 as *void, "text", "caption", 0u);
    return 0;
}
