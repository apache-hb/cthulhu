using stdio(printf);

def fib(i: int): int = (i == 0 || i == 1) ? i : fib(i - 1)  + fib(i - 2);

def main(argc: int, argv: **char): int {
    var result: int = fib(10);

    printf("fib(10) = %d\n", result);

    return 0;
}
