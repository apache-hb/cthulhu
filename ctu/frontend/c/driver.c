#include "driver.h"

c_t *c_parse(reports_t *reports, file_t *file) {
    (void)reports;
    (void)file;
    return NULL;
}

lir_t *c_analyze(reports_t *reports, c_t *node) {
    (void)reports;
    (void)node;
    return NULL;
}
