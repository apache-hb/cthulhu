def main($: int, b: int, $: bool): int {
    final $ = b;

    return 0;
}

// hello world
def a(): bool {
    final x = main(0, 200, false);

    if x == 200 {
        final y = x;
    }
    final y = x + x / x;

    final z = x * 100;

    final $ = -z;

    return y != 0;
}
