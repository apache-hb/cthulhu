using a1 = union enum {
    a -> u64
}

using a2 = union enum: u64 {
    a -> u64
}

using a3 = union enum: [u8] {
    a -> u32,
    b -> u64,
    c -> (u32, u64)
}

using a4 = union {
    a: u64,
    b: u32,
    c: (u64, u64)
}

using a5 = union (u64, u32, [u8])