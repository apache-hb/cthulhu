module std;

@extern
export def malloc(size: usize): *void;

@extern 
export def realloc(ptr: *void, size: usize): *void;

@extern
export def free(ptr: *void): void;

@extern
export def puts(s: str): int;

@extern
export def exit(code: int): void;

export const null = __null;
