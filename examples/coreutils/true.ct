using stdrt(stdio, env, exit);

var help = """
Usage: true [ignored command line arguments]
or: true OPTION
   Exit with a status code indicating success.    
""";

def main {
    if env::args().size() == 2 {
        stdio::puts(help);
    }

    exit(0);
}
