module cthulhu::stdlib;

@extern("malloc")
export def malloc(n: usize): opaque;

@extern("free")
export def free(ptr: opaque): void;

@extern("memcpy")
export def memcpy(dst: opaque, src: opaque, n: usize): opaque;

@extern("memset")
export def memset(dst: opaque, c: uchar, n: usize): opaque;
