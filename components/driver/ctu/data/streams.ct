object BasicStream {
    def offset(self: *BasicStream): usize = virtual;
    def seek(self: *BasicStream, offset: usize): bool = virtual;
}

object InStream(super: BasicStream) {
    def read(self: *InStream, bytes: usize, ptr: opaque): usize = virtual;
}

object OutStream(super: BasicStream) {
    def write(self: *OutStream, bytes: usize, ptr: opaque): usize = virtual;
}


object InOutStream(in: InStream, out: OutStream) {
    
}
